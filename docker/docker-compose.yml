services:
  docker-site:
    image: nginx:alpine
    container_name: docker_khassam_nginx
    restart: unless-stopped
    ports:
      - "127.0.0.1:8083:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
